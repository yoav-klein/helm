# using the helpers file
---
You can outsource the named templates defined with `define` to the `_helpers.tpl` file.
Those templates will be available from all the templates in the templates directory
