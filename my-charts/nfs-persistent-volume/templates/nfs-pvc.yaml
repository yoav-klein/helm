apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-pvc
  namespace: nfs-pv
spec:
  accessModes:
  - ReadOnlyMany
  storageClassName: ""
  resources:
     requests:
       storage: 5Mi
       
